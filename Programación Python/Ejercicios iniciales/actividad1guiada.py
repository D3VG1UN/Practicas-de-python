#1. Ventajas que tiene este editor: no tienes que moverte entre dos pantallas: 
#   la terminal aparece en el mismo lugar que el código.
#2. otra ventaja es que puedes seleccionar el botón rojo en una línea para comprobar 
#   si se produce algún error en esa línea
#3. Además, gracias al Python Debugger, en esta plataforma se te autocompleta
#   con recomendaciones de lo que quieres escribir, algo que no pasa en el IDLE

var1=float(input("introduce un número: "))
var2=float(input("introduce otro número: "))

total=var1+var2

print("La suma de las variables es de: ",total)